# Minimal requirements for AI Voice Code Assistant
# This version avoids all problematic dependencies

# Core (absolutely required)
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
requests==2.31.0
pydantic==2.4.2

# For form data
python-multipart==0.0.6

# API clients (for AI services)
groq==0.4.1
openai==1.3.5

# Git integration
GitPython==3.1.40

# Async support
aiohttp==3.9.1
websockets==12.0

# Basic testing
pytest==7.4.3

# That's it! No torch, no transformers, no complex ML dependencies